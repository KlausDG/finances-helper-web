import { CategoryFormData } from "@/concepts/Categories/types";
import { UseFormSetValue } from "react-hook-form";

export type IconsSectionProps = {
  selectIcon: UseFormSetValue<CategoryFormData>;
};
